import styled from "styled-components";
import { motion } from "framer-motion";

export const UpdateCardContainer = styled(motion.div)`
  width: 60vw;
  height: 40rem;
  overflow: hidden;
  box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.2);
  padding: 1rem;
  transition: 0.4s all;
  border-radius: 2rem;
  margin: 1rem;
  background-color: #ececec;
`;